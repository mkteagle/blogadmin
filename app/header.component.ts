import { Component } from '@angular/core';

@Component({
    selector: 'ng-heading',
    templateUrl: '../templates/heading.html'
})

export class NgHeadingComponent {
    sectionHeading: string = 'Blog Editor';
}

